# The P0 wave, 6 September 2026

Justin approved the fourteen reports (research/2026-09-05-master-audit.md,
merged in PR 975) and said go. This plan is the P0 section of Report 14,
plus the one P1 item he approved directly (the casting call). Lane: schools
product. Migration numbers claimed: 257 and 258 (highest on main is 255,
open PR 978 claims 256).

## The work

1. MAKE EVERY SENTENCE TRUE (copy only). The audit's biggest exposure:
   copy in four places sells delivery recording, live coverage and per
   pupil judgements that no code implements. Rewrite to describe the paper
   loop and the privacy posture we actually ship:
   - schools/app/hub/faq/page.tsx (the data question and the Ofsted
     question)
   - schools/app/hub/data-protection/page.tsx (the "first name and
     initial... delivery records" claim)
   - schools/app/page.tsx (the two "coverage record" home page lines)
   - shared/components/LessonPlayer.tsx (the end of lesson "one tap on
     the register" line and the showcase upsell's "progress evidence for
     every child")
2. KS3-12 TO STANDARD (migration 257). The weakest module on the most
   statutory ground gets its worksheet: six items with expected_verdict
   and teaching_point in the house pattern, plus the worksheet block.
   Scripts stay as they are in this migration; a fuller script deepening
   rides with the gold standard exemplar pass (P2) so content quality
   changes are reviewed once, not twice.
3. STATUTORY NAMING PASS (copy). The RSHE materials viewing route named
   in the parents pack; the withdrawal rules paragraph; a GDC PROVIDES
   versus SCHOOL RESPONSIBILITY section in the FAQ.
4. PLAYER ACCESSIBILITY PASS. Global prefers-reduced-motion in the
   player, a polite live region announcing slide changes, authored focus
   styles, labels on the interactives, and an accessibility statement in
   the Hub.
5. TITLE CHARACTERS KS1 TO KS3 (migration 258, approved by Justin).
   Pebble (celebrate) hosts KS1, Bloop (dance) KS2, Orbit (football) KS3,
   matching the family's own key stage mapping in intro-characters.ts.
   Same shape as migration 233: title slide found by type, snapshot
   first, RLS on the backup, idempotent.

## Not in this wave

The two register checks (Justin's browser). The recording decision
(P1, after the first paying school). Interactives into the spiral,
learning record to 21, CPD expansion, SEND adaptations (P1, next).
